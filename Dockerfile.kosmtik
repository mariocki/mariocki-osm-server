FROM debian:testing-slim

RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone

# Set up renderer user
RUN adduser --disabled-password --gecos "" renderer

RUN apt-get update
RUN apt-get install -y screen vim sudo wget gnupg2 apt-transport-https ca-certificates curl

#install nodejs
RUN wget --quiet -O - https://deb.nodesource.com/setup_14.x | bash - \
  && apt-get update \
  && apt-get install -y nodejs

# Style dependencies
RUN apt-get update \
  && apt-get install --no-install-recommends -y \
  git-core \
  gnupg \
  postgresql-client \
  python3 \
  python3-distutils \
  fonts-hanazono \
  fonts-noto-cjk \
  fonts-noto-hinted \
  fonts-noto-unhinted \
  mapnik-utils \
  ttf-unifont \
  unzip

# Kosmtik with plugins, forcing prefix to /usr
# taken from https://github.com/kosmtik/kosmtik/issues/302
RUN npm set prefix /usr \
  && npm install --unsafe-perm -g git+https://github.com/kosmtik/kosmtik \
  && npm install -g leaflet \ 
  && npm install -g leaflet-editinosm \
  && npm install -g leaflet.photon \
  && npm install -g osmtogeojson \
  && npm install -g git+https://github.com/kosmtik/kosmtik-fetch-remote \ 
  && npm install -g git+https://github.com/kosmtik/kosmtik-overlay \ 
  && npm install -g git+https://github.com/kosmtik/kosmtik-map-compare \ 
  && npm install -g git+https://github.com/kosmtik/kosmtik-osm-data-overlay \ 
  && npm install -g git+https://github.com/kosmtik/kosmtik-mapnik-reference \ 
  && npm install -g git+https://github.com/kosmtik/kosmtik-open-in-josm \ 
  && npm install -g git+https://github.com/kosmtik/kosmtik-geojson-overlay 

COPY ./scripts/kosmtik/kosmtik.yml /tmp/.kosmtik-config.yml

RUN apt-get clean autoclean \
  && apt-get autoremove --yes \
  && rm -rf /var/lib/{apt,dpkg,cache,log}

ADD "https://www.random.org/cgi-bin/randbyte?nbytes=10&format=h" skipcache

# Start running
COPY ./scripts/kosmtik/run.sh /
RUN chmod +x /run.sh
ENTRYPOINT ["/run.sh"]
CMD []
